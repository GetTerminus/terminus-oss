import { InjectionToken } from '@angular/core';

import { TsSearchDefaultOptions } from './ui-search.module';

export const TS_SEARCH_DEFAULT_OPTIONS = new InjectionToken<TsSearchDefaultOptions>('TS_SEARCH_DEFAULT_OPTIONS');
