import { create } from '@storybook/theming/create';

import logo from './assets/terminus-logo.svg';

export default create({
  base: 'light',
  brandTitle: 'Terminus Platform UI',
  brandUrl: '/',
  brandImage: logo,
});
